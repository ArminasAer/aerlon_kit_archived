// import type { PageServerLoad } from './rng/$types';

// export const load = (async () => {
// 	return {};
// }) satisfies PageServerLoad;

export const prerender = false;
export const ssr = true;
export const csr = true;
